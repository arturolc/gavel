version: 0.2
phases:
  install:
    commands:
      - apt update 
      - apt install docker -y
      - curl -L https://github.com/docker/compose/releases/download/1.21.2/docker-compose-`uname -s`-`uname -m` -o /usr/local/bin/docker-compose
      - chmod +x /usr/local/bin/docker-compose
      - docker-compose --version
  pre_build:
    commands:
      - echo DB_PASSWORD=2YsaTLxtzQzorB6rq2Qmpr@3mLsRGN29 >> .env
      - echo f1c2a01955cd206c25faa0d8b4a8b41a6b6c0674 >> ~/tok.txt
      - cat ~/tok.txt | docker login https://docker.pkg.github.com -u arturolc --password-stdin
  build:  
    commands:
      - docker-compose up
  post_build:
    commands:
      - echo "No test to run"